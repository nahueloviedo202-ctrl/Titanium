

{%- liquid
  assign card = block.settings
  assign product = card.product

  assign card_image = card.image
  if card_image == blank and product != blank
    assign card_image = product.featured_image
  endif

  assign card_title_text = card.title
  if card_title_text == blank and product != blank
    assign card_title_text = product.title
  endif
  if card_title_text != blank
    assign card_title = card_title_text
  else
    assign card_title = 'placeholders.product_title' | t
  endif

  assign card_link = card.link
  if card_link == blank and product != blank
    assign card_link = product.url
  endif

  assign product_on_sale = false
  if product != blank and product.compare_at_price > product.price
    assign product_on_sale = true
  endif

  assign card_compare_price = card.compare_price
  if card_compare_price == blank and product_on_sale
    capture card_compare_price
      render 'format-price', price: product.compare_at_price
    endcapture
  endif

  assign card_price = card.price
  if card_price == blank and product != blank
    capture card_price
      render 'format-price', price: product.price
    endcapture
  endif

  assign rating_value = card.rating_value | at_most: 5 | at_least: 0
-%}

{% render 'category-nav-product-card',
  image: card_image,
  title: card_title,
  link: card_link,
  open_in_new_tab: card.open_in_new_tab,
  sale_badge_text: card.sale_badge_text,
  show_rating: card.show_rating,
  rating_value: rating_value,
  review_count: card.review_count,
  bullet_1_icon: card.bullet_1_icon,
  bullet_1_label: card.bullet_1_label,
  bullet_2_icon: card.bullet_2_icon,
  bullet_2_label: card.bullet_2_label,
  compare_price: card_compare_price,
  price: card_price,
  save_badge_text: card.save_badge_text,
  block: block
%}

{% schema %}
{
  "name": "t:names.header_category_nav_product",
  "tag": null,
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "t:settings.product",
      "info": "t:info.category_nav_product_source"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "t:settings.image"
    },
    {
      "type": "text",
      "id": "title",
      "label": "t:settings.title"
    },
    {
      "type": "header",
      "content": "t:content.block_link"
    },
    {
      "type": "url",
      "id": "link",
      "label": "t:settings.link"
    },
    {
      "type": "checkbox",
      "id": "open_in_new_tab",
      "label": "t:settings.open_new_tab",
      "default": false
    },
    {
      "type": "header",
      "content": "t:content.badges"
    },
    {
      "type": "text",
      "id": "sale_badge_text",
      "label": "t:settings.image_badge_text",
      "info": "t:info.category_nav_image_badge"
    },
    {
      "type": "text",
      "id": "save_badge_text",
      "label": "t:settings.price_badge_text",
      "info": "t:info.category_nav_price_badge"
    },
    {
      "type": "header",
      "content": "t:content.rating"
    },
    {
      "type": "checkbox",
      "id": "show_rating",
      "label": "t:settings.show_rating",
      "default": true
    },
    {
      "type": "range",
      "id": "rating_value",
      "label": "t:settings.rating_value",
      "min": 0,
      "max": 5,
      "step": 0.5,
      "default": 5,
      "visible_if": "{{ block.settings.show_rating }}"
    },
    {
      "type": "text",
      "id": "review_count",
      "label": "t:settings.review_count",
      "default": "t:text_defaults.header_category_nav_review_count",
      "visible_if": "{{ block.settings.show_rating }}"
    },
    {
      "type": "header",
      "content": "t:content.icons"
    },
    {
      "type": "image_picker",
      "id": "bullet_1_icon",
      "label": "t:settings.image_icon"
    },
    {
      "type": "text",
      "id": "bullet_1_label",
      "label": "t:settings.label",
      "default": "t:text_defaults.header_category_nav_bullet_1"
    },
    {
      "type": "image_picker",
      "id": "bullet_2_icon",
      "label": "t:settings.image_icon"
    },
    {
      "type": "text",
      "id": "bullet_2_label",
      "label": "t:settings.label"
    },
    {
      "type": "header",
      "content": "t:content.pricing"
    },
    {
      "type": "text",
      "id": "compare_price",
      "label": "t:settings.compare_price"
    },
    {
      "type": "text",
      "id": "price",
      "label": "t:settings.current_price"
    }
  ],
  "presets": [
    {
      "name": "t:names.header_category_nav_product"
    }
  ]
}
{% endschema %}
